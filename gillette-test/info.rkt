#lang info

(define collection "tests")

(define build-deps '("base"
                     "gillette-lib"))

(define update-implies '("gillette-lib"))

(define pkg-desc "Tests for Gillette")

(define pkg-authors '("jesse@serverracket.com"))

(define version "0.2")
